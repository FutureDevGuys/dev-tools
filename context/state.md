# Active work

- Publish and install dev-auth v0.3.8 with provider-selected owner-scope authority for transparent GitHub CLI workloads; preserve v0.3.7 as the rollback line until the successor passes the standard two-pass strong setup harness and live owner-scope probe.
- Integrate the bounded release publisher, then use its admitted operation-only path to publish the prepared Update All 0.1.6 generation 7 and sync-configs 0.1.13 release set without exporting the release key or using ambient human GitHub credentials.
- Prove Linux strong-mode admission and transparent human passthrough across CLI, desktop, subagents, fresh shells, editors, signals, sandbox adapters, cache expiry, revocation, and no-human-fallback before changing normal launcher resolution.
- Run native macOS, Windows, and WSL acceptance before advertising those platforms as supported; their backends remain interface-only until native evidence exists.
- Integrate the product-neutral one-shot privilege authorizer with a receipt-owned Dev Auth setup helper; retain Unix sudo as the first backend and keep Windows UAC and macOS Authorization Services unsupported until native implementations and acceptance exist.
