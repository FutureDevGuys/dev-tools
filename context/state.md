# Active work

- Native Windows and WSL runtime acceptance remains required before those platforms are advertised as supported.
- Dev Cache 0.1.5 must be published and activated before the current Linux cache root can reconcile its proven previous-generation workspace identities. Long-running commands started by 0.1.4 retain the old root-wide lease until their host process exits; do not interrupt them. After activation and a normal host restart, apply the bounded GC plan and require a clean doctor report.
