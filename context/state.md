# Active work

- `dev-auth` requires implementation, Linux post-login acceptance, Syscfg provisioning, and a release before unattended repository use is enabled.
- Native Windows and WSL runtime acceptance remains required before those platforms are advertised as supported.
