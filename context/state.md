# Active work

- Linux managed Git routing rejects ordinary symlinked caller `PATH` entries such as `/bin`, even though managed execution replaces `PATH`; publish a signed successor after preserving unmanaged managed-root detection and passing the full release gate.
- Run native macOS, Windows, and WSL acceptance for enrollment, Git, bounded `gh`, SSH authentication/signing, cache expiry, concurrency, revocation, and no-human-fallback before advertising those environments as fully supported.
