# Active work

- Execute the accepted sequence and release gates in [Dev Tools completion plan](docs/dev-tools-completion-plan.md); keep this file limited to incomplete outcomes and action-changing risks.
- Close issue 40 and attach acceptance evidence once the existing GitHub integration has Issues write authority for FutureDevGuys/dev-tools. Its admitted closeIssue operation is denied as "Resource not accessible by integration"; do not substitute credentials or widen private policy here. Check final issue/comment state before retrying.
- Publish the authenticated narrowed generation-2 registry transaction: exact unchanged privilege/product/reconcile-protocol 0.1.0 archives from retained clean 46066ec source. Preserve the original six-package signed inventory as evidence only; its installation/secret bytes must never be uploaded.
- Publish authenticated corrected generation-3 command 0.1.1, installation 0.2.0 and secret 0.1.1 packages from clean source bf836aa. Do not reuse original signed version identities for changed bytes.
- Publish dependency-ready registry batches; private bootstrap input remains required. Publish dependent completion/release/update batches only after exact registry dependencies resolve, verify checksums, configure Trusted Publishing and revoke the bootstrap token. Release/update must include integrated redirect admission rather than frozen implementation bytes.
- Resolve the reported consumer need for bounded regular-file public stdout capture in dev-tools-command without duplicating its Unix process-group supervisor or changing secret-bearing pipe capture. Current 0.1.1 has no selector; distinguish hard storage bounds from polled-size limits and qualify upstream truncation with a public synthetic fixture before choosing the interface.
- Retain the clean detached dev-tools-main worktree until its exact registry transaction is resolved; then recheck cleanliness/dependencies and remove it through normal worktree removal.
- Resolve the open reservation-reclamation preference and finish Artifact Update retained reservation lifecycle; preserve unmarked legacy temporaries without independent ownership evidence. Publish installation 0.2.1 with the recoverable-publication APIs before dependent Artifact Update registry use. Complete native cache/configuration storage backends and signed online/offline/install/rollback/crash acceptance; cached observations never grant release or installation authority.
- Publish product 0.1.1 and update 0.2.0 with the v2 mutation-result and separate artifact-preparation contracts before registry-dependent adapter adoption; keep frozen product 0.1.0 bytes unchanged.
- Complete product-owned dev-tools-update adapters and the common doctor/update contract. Ship Skills Sync repair before read-only doctor and remove its bounded legacy online manifest exception only after the source-bound successor is accepted.
- Complete Update All first-use ancestor durability, installation/state crash recovery, mixed-version writer cutover and retained signed-proof integration before adapting its legacy release functions to the common loop; qualify and release its bounded state/cache custody and shared pre-contact HTTPS admission with the source-bound successor.
- Complete release-binary five-shell completion acceptance, including Fish's subcommand-shaped option-value ambiguity and Elvish's leading-subcommand path limits, before claiming full grammar/platform support.
- Retire Update All's temporary Python package_authority bridge only after the separate private consumer owner supplies an accepted native replacement interface; do not extend the bridge or implement private caller migration here.
- Complete smart binding execution, journaled activation, target refresh/rebind and receipt-owned rollback under ADRs 0009/0017. Qualify path-sensitive scripts, native arguments/streams, environment/cwd, proxy cycles and setup approval before activation.
- Implement Dev Auth administrator-policy v3 logical secret names and typed read/public/exec operations with independently qualified provider/projection custody; operation-only private keys remain nonexportable.
- Complete native privilege-session identity/authority, conserved delegation, stream DAGs and expiry/revocation termination beyond the lifecycle foundation before exposing product session operations.
- Retain exact Dev Auth 0.3.11 verification compatibility until its receipt-owned rollback window closes; never reintroduce legacy construction or reissue accepted bytes.
- Complete Linux strong-mode admission and transparent human passthrough across CLI/desktop, fresh shells, editors, nested workloads, signals, sandbox adapters, expiry/revocation and denied-session no-fallback behavior.
- Obtain real macOS and Windows/WSL hosts, SDKs and required signing/entitlements. The Apple cross-check still requires a real SDK providing TargetConditionals.h; cross-compilation does not establish native acceptance.
- Keep broad validation within available build capacity using bounded jobs/debug/incremental settings; preserve sources, frozen publication inputs and release provenance during scoped output cleanup.
