# Active work

- Update All 0.1.5 is prepared in source but not released. Complete the routine
  release with the authorized release key, manifest generation 6, the nested
  `update-all/v0.1.5` tag, and exact installed-artifact verification; the
  1Password approval boundary is unavailable while the operator is away.
- Native Windows and WSL runtime acceptance remains required before those platforms are advertised as supported.
