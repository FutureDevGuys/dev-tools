# Active work

- Native Windows and WSL runtime acceptance remains required before those platforms are advertised as supported.
- Dev Cache 0.1.6 must be published and activated to finish reconciling two payload-free unknown-generation workspace duplicates that contain empty cache-directory scaffolding. Apply the bounded GC plan after activation and require a clean doctor report.
