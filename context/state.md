# Active work

- Build, sign, publish and install Dev Cache 0.1.7 for issue 40's compiler-intercept maintenance correction and the read-only diagnostic/root-probe corrections; verify real compiler probes, representative builds and nonmutating diagnostics before closing the GitHub issue.

- Complete release-level completion shell/platform acceptance and broader grammar qualification, including Fish's inherited subcommand-shaped option-value ambiguity and Elvish's inherited leading-subcommand path limits; publish the new `dev-tools-completion` crate from signed integrated source before external adoption. Do not infer runtime support from generation or cross-compilation.

- Build capacity remains constrained; retain bounded debug/incremental settings for broad validation and preserve sources, frozen registry packages and release provenance during any scoped build-output cleanup.

- Complete abandoned-staging recovery and remaining native cache backends without creating another worktree. Reused metadata must be reselected under current local configuration; failed refresh must not advance cache state. HTTP validators and receipt-content observations never grant release authentication or installation authority.
- Complete the Apple artifact-update cross-check with a real configured Apple SDK: target-specific Clang/LLVM tools get past the default GCC mismatch, but `ring` requires `TargetConditionals.h`. Do not fabricate SDK headers or treat a cross-check as native acceptance; independent source work remains available.
- Older unmarked staging/cache files, interrupted metadata-publication files, unpublished initial-directory temporaries and retained reservation eviction still need an ownership-safe recovery design; do not infer deletion authority from names, ages or PIDs. Preserve explicit first-use semantics and ledger/cache independence. Native acceptance of trust/check/status/install remains required before release, including signed HTTPS installation, offline reuse, crash recovery and steady-state repeat operation.
- Include the additive cancellable public-input and retained-executable read-handle command APIs in a signed registry generation before downstream consumers use them; retain public-only byte staging and platform-specific process cleanup, without adding a private process controller.

- Release and install native sync-configs with the additive pre/post hook metadata contract before downstream profile-aware hooks depend on it; complete full consumer repeat convergence after activation.
- Remove the remaining Dev Auth 0.3.11 verification compatibility only after its receipt-owned rollback window closes; never reintroduce legacy manifest construction or reissue an accepted version.
- Publish the signed first shared-crate batch under `/home/rashino/.cache/dev-tools-registry-staging/shared-46066ec.eRFZhD` from clean source `46066ec96d2e527c397d2e197a39d9955fa6a485`; private bootstrap-token input remains required. Then package/sign/publish dependent shared crates in registry-visible dependency order, verify exact registry checksums, replace the downstream exact-Git probe, configure Trusted Publishing, and revoke the bootstrap token.
- Build the later `dev-tools-release`/`dev-tools-update` registry batch from integrated redirect-admission source rather than the frozen first-batch source; the first six signed archives do not contain this transport and remain unchanged. Include URL-parser dependency and binary-size impact in the release gate.
- Requalify staged `dev-tools-installation` and `dev-tools-secret` before upload against their nonblocking non-regular-file open and rejected-secret-input zeroization corrections. Preserve the original signed batch as evidence, but do not upload known unfixed archives; reconcile successor package/manifest generation with registry and accepted signing state before replacing any planned publication bytes.
- Use the admitted operation-only release path to rebuild, publish, install, and accept Update All 0.1.6 generation 7 and native sync-configs 0.2.0 generation 14 from the final merged source; the earlier `acd45e4` artifacts are superseded and must not be published.
- Run the current Syscfg manifest through installed native sync-configs twice and require the second pass to perform no configuration mutations or unnecessary authentication.
- Replace Update All's optional Python `package_authority` bridge only after Syscfg owns and accepts an equivalent native package catalog/executor; until then the bridge is a known temporary violation of the public/private dependency boundary and must not gain new behavior.
- Migrate every public product onto `dev-tools-update` with product-owned authenticated cache and installation adapters, then complete the common doctor/update contract without sibling-product runtime dependencies. Ship and accept Skills Sync's explicit `repair` spelling, migrate callers, and qualify the later read-only `doctor` cutover under ADR 0014; the current doctor remains mutating.
- Replace the remaining Python release scripts with native `release-admin`, port their acceptance corpus first, and delete the Python path only after deterministic build, signing, publication, anonymous verification, and rollback acceptance.
- Complete generic smart workload bindings with receipt-owned continuation/structured/pinned-shell targets, digest-bound target refresh and explicit rebind, immutable rollback generations, and setup-plan integration; qualify the v2 target-specific draft-plan cutover under ADR 0017 and complete native argument/stream forwarding, environment/cwd policy and receipt-owned proxy-cycle checks before activation. Descriptor execution changes a script's observed pathname, so qualify path-sensitive wrapper/resource behavior before adopting it as a transparent continuation. Keep existing Codex and Claude wrappers as downstream continuations.
- Add administrator-policy v3 logical secret names and typed read/public/exec operations on the provider-neutral adapter without exposing operation-only private keys.
- Complete `dev-tools-privilege-session` beyond its local lifecycle with native identity-bound typed/exact-plan/unrestricted leases, conserved recursive delegation, stream DAGs, expiry-driven native termination, and product CLI integration; never treat the pure state machine as authority or refresh global sudo state.
- Complete `artifact-update` remaining source adapters, abandoned-staging recovery and native configuration-storage backends; qualify the common doctor-result cutover under ADR 0016 before release, including schema-consumer migration. Do not publish or claim complete/native-platform acceptance for its configuration/selection foundation.
- Remove the retained `/home/rashino/repos/dev-tools-main` source worktree after the exact `46066ec` registry transaction finishes; keep it clean and detached while canonical development proceeds on main.
- Prove Linux strong-mode admission and transparent human passthrough across CLI, desktop, subagents, fresh shells, editors, signals, sandbox adapters, cache expiry, revocation, and no-human-fallback before making the full support claim.
- Obtain user-owned native macOS and Windows/WSL2 test-host access and required signing/entitlements; full completion requires their acceptance as well as Linux. Cross-compilation cannot close these gates.
